import 'package:flutter/material.dart';

class TabletAccount extends StatelessWidget {
  const TabletAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}