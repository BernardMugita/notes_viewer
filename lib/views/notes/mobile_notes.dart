import 'package:flutter/material.dart';

class MobileNotes extends StatelessWidget {
  const MobileNotes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
