import 'package:flutter/material.dart';

class TabletNotes extends StatelessWidget {
  const TabletNotes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
